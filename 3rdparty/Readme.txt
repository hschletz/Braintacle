This directory contains third party libraries that are not installable via composer.

"Db" is a fork of laminas/laminas-db which is effectively abandoned and does not
support recent PHP versions. The code is patched as needed to support these
versions. Only a subset of files is bundled here. The rest of the original files
are not used by Braintacle.
